# 인증 클라이언트 환경 설정 파일
# 이 파일을 .env.local로 복사하고 환경에 맞게 값을 수정하세요

# ========================================
# 서버 URL 설정
# ========================================

# 인증 서버 URL - 백엔드 auth-server API 주소
NEXT_PUBLIC_AUTH_SERVER_URL=http://localhost:8000/api

# ========================================
# SSO 통합 설정
# ========================================

# SSO 도메인 설정 (쿠키 도메인)
NEXT_PUBLIC_DOMAIN=localhost

# 인증 클라이언트 URL (현재 앱) - SSO 허브 URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# 타겟 서비스 URL (SSO 리다이렉트 대상)
NEXT_PUBLIC_PORTAL_CLIENT_URL=http://localhost:3200
NEXT_PUBLIC_PORTAL_ADMIN_URL=http://localhost:3210

# ========================================
# 환경 설정
# ========================================

# 애플리케이션 환경 (development, production)
NODE_ENV=development
NEXT_PUBLIC_ENVIRONMENT=development

# ========================================
# Next.js 설정
# ========================================

# Next.js 텔레메트리 비활성화 (프라이버시 보호)
NEXT_TELEMETRY_DISABLED=1

# ========================================
# 선택적 설정 (필요시 주석 해제)
# ========================================

# 디버그 모드 활성화 여부 (true/false)
# NEXT_PUBLIC_DEBUG=false

# 로그 레벨 설정 (error, warn, info, debug)
# NEXT_PUBLIC_LOG_LEVEL=info

# ========================================
# 프로덕션 환경 배포시 참고사항
# ========================================
# 실제 운영 환경에서는 다음과 같이 실제 도메인으로 변경하세요:

# 서버 URL 설정
# NEXT_PUBLIC_AUTH_SERVER_URL=https://auth-server.yourdomain.com/api
# NEXT_PUBLIC_DOMAIN=yourdomain.com
# NEXT_PUBLIC_APP_URL=https://auth.yourdomain.com
# NEXT_PUBLIC_PORTAL_CLIENT_URL=https://portal.yourdomain.com
# NEXT_PUBLIC_PORTAL_ADMIN_URL=https://admin.yourdomain.com
# NODE_ENV=production
# NEXT_PUBLIC_ENVIRONMENT=production

# HTTP 클라이언트 프로덕션 설정 (@krgeobuk/http-client)
# ※ 아래 설정들은 기본값이 있어 로컬에서는 생략 가능하지만, 운영에서는 명시적 설정 권장

# 허용된 도메인 목록 (보안 정책용) - 쉼표로 구분
# 기본값: localhost,127.0.0.1,krgeobuk.com → 운영 도메인으로 변경 필요
# ALLOWED_ORIGINS=yourdomain.com,auth.yourdomain.com,portal.yourdomain.com,admin.yourdomain.com

# API 요청 타임아웃 시간 (밀리초) - 기본값: 10000ms → 인증 서비스는 빠른 응답 필요
# NEXT_PUBLIC_API_TIMEOUT=8000

# 레이트 리미터 설정 - 인증 서비스는 보안상 더 엄격하게 제한
# 최대 요청 횟수 (기본값: 100) → 인증 서비스는 30 권장 (무차별 대입 공격 방지)
# NEXT_PUBLIC_RATE_LIMIT_MAX_ATTEMPTS=30
# 시간 윈도우 (기본값: 60000ms)
# NEXT_PUBLIC_RATE_LIMIT_WINDOW_MS=60000

# 보안 설정 (모든 기본값: true) - 인증 서비스는 최고 보안 수준 필요
# CSRF 보호 활성화 (필수)
# NEXT_PUBLIC_ENABLE_CSRF=true
# 입력값 검증 활성화 (필수)
# NEXT_PUBLIC_ENABLE_INPUT_VALIDATION=true
# 보안 로깅 활성화 (필수 - 보안 감사용)
# NEXT_PUBLIC_ENABLE_SECURITY_LOGGING=true